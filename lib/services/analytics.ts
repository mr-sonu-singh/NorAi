export function initAnalytics(analyticsId?: string): void {
  if (!analyticsId) return;
  // Deferred, privacy-safe analytics initialization
}

export function trackEvent(_eventName: string, _properties?: Record<string, unknown>): void {
  // Event tracking stub
}
