import { NavigationGroup } from './NavigationGroup';

describe('NavigationGroup Molecule', () => {
  it('defines NavigationGroup component correctly', () => {
    expect(NavigationGroup).toBeDefined();
  });
});
