import { NavigationMenu } from './NavigationMenu';

describe('NavigationMenu Molecule', () => {
  it('defines NavigationMenu component correctly', () => {
    expect(NavigationMenu).toBeDefined();
  });
});
