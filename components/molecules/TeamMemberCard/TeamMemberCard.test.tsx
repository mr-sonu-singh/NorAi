import { TeamMemberCard, TeamMemberCardBody } from './TeamMemberCard';

describe('TeamMemberCard Molecule', () => {
  it('defines TeamMemberCard and TeamMemberCardBody correctly', () => {
    expect(TeamMemberCard).toBeDefined();
    expect(TeamMemberCardBody).toBeDefined();
  });
});
