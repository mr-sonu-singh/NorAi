import { TestimonialCard, TestimonialCardBody } from './TestimonialCard';

describe('TestimonialCard Molecule', () => {
  it('defines TestimonialCard and TestimonialCardBody correctly', () => {
    expect(TestimonialCard).toBeDefined();
    expect(TestimonialCardBody).toBeDefined();
  });
});
