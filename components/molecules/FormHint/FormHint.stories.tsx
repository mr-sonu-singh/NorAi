import { FormHint } from './FormHint';

const meta = {
  title: 'Molecules/FormHint',
  component: FormHint,
};

export default meta;

export const Default = {
  args: {
    text: 'We will never share your email with third parties.',
  },
};
