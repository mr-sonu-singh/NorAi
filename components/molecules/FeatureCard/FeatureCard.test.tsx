import { FeatureCard, FeatureCardBody } from './FeatureCard';

describe('FeatureCard Molecule', () => {
  it('defines FeatureCard and FeatureCardBody correctly', () => {
    expect(FeatureCard).toBeDefined();
    expect(FeatureCardBody).toBeDefined();
  });
});
