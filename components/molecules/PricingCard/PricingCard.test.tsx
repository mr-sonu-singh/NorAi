import { PricingCard, PricingCardBody } from './PricingCard';

describe('PricingCard Molecule', () => {
  it('defines PricingCard and PricingCardBody correctly', () => {
    expect(PricingCard).toBeDefined();
    expect(PricingCardBody).toBeDefined();
  });
});
