import { ModalHeader } from './ModalHeader';

const meta = {
  title: 'Molecules/ModalHeader',
  component: ModalHeader,
};

export default meta;

export const Default = {
  args: {
    title: 'Confirm Model Deployment',
    description: 'Review deployment parameters before pushing to production servers.',
  },
};
