import { Checkbox } from './Checkbox';

describe('Checkbox Atom', () => {
  it('supports checked and indeterminate states with mixed aria-checked', () => {
    expect(Checkbox).toBeDefined();
  });
});
