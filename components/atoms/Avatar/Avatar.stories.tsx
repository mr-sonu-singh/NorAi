import { Avatar } from './Avatar';

const meta = {
  title: 'Atoms/Avatar',
  component: Avatar,
};

export default meta;

export const InitialsFallback = {
  args: {
    fallback: 'JD',
    size: 'md',
    status: 'success',
  },
};
