import { Pill } from './Pill';

const meta = {
  title: 'Atoms/Pill',
  component: Pill,
};

export default meta;

export const Default = {
  args: {
    children: '12 Updates',
    variant: 'accent',
  },
};
