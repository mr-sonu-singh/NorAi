import { Spinner } from './Spinner';

const meta = {
  title: 'Atoms/Spinner',
  component: Spinner,
};

export default meta;

export const Default = {
  args: {
    size: 'md',
    'aria-label': 'Loading data',
  },
};
