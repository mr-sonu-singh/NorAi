import { ProgressBar } from './ProgressBar';

const meta = {
  title: 'Atoms/ProgressBar',
  component: ProgressBar,
};

export default meta;

export const HalfWay = {
  args: {
    value: 50,
    max: 100,
  },
};
