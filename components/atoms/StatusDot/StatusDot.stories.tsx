import { StatusDot } from './StatusDot';

const meta = {
  title: 'Atoms/StatusDot',
  component: StatusDot,
};

export default meta;

export const Success = {
  args: {
    status: 'success',
    pulse: true,
  },
};
